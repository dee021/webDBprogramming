<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <h1>검색화면</h1>

    <form action="/<%= path %>/search" method="post">
        <div class="row mb-3">
            <label for="inputEmail3" class="col-sm-2 col-form-label"><%= kind %></label>
            <div class="col-sm-5">
                <input type="id" class="form-control" placeholder="제목을 입력하세요" name="keyword" maxlength="50">
            </div>
        </div>
        <button type="submit" class="btn btn-primary">검색</button>
    </form>
    <p>
        <% if (listyn == "Y")
        {
            %>
        <table class="table">
            <% var i =0;
            while(i < results.length) {
                %>
            <tr><td><%= results[i].title%></td><td><%= results[i].description%></td></tr>
        <%
        i++;
            }
            %>
            </table>
            <% 
        } %>
    </p>
</body>
</html>