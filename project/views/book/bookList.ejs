<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
</head>
<body>
    <h1>도서 목록</h1>
    <table class="table table-sm table-bordered" style="width:1100px">
    <%
    var i = 0;
    while(i < results.length) {
        %>
        <tr><td class="td td-sm"><%= results[i].name%></td>
        <td><%= results[i].publisher%></td>
        <td><%= results[i].author%></td>
        <td><%= results[i].stock%></td></tr>
<tr>
        <td><%= results[i].pubdate%></td>
        <td><%= results[i].pagenum%></td>
        <td><%= results[i].ISBN%></td>
        <td><%= results[i].ebook%></td></tr><tr>

        <td><%= results[i].kdc%></td>
        <td><%= results[i].img%></td>
        <td><%= results[i].price%></td>
        <td><%= results[i].nation%></td></tr>
        <tr>
        <td colspan="2"><%= results[i].description%></td>
    <td><a href="/book/update/<%=results[i].id%>">update</a></td>
    <td><a href="/book/delete_process/<%=results[i].id%>">delete</a></td>
</tr>
<%
i++;
    }
    %>
    </table>
</body>
</html>